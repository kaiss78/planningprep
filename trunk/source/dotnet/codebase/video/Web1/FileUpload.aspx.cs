using System;
using System.Collections.Generic;
using System.Data;
using System.Web.UI;
using App.Util;

public partial class _FileUpload : Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        //string filePath = Server.MapPath(ConfigReader.InputExel);;
        //string xmlDir = Server.MapPath(ConfigReader.XmlDir);;
        //List<VideoSectionItem> videoSectionItems = ExelHelper.Instance.GetDataFromExcel(filePath);
        //int levelCount = ExelHelper.Instance.GetLevelCount(videoSectionItems);
        //XmlHelper.Instance.GenerateXmlFiles(videoSectionItems, xmlDir,levelCount);
    }
}